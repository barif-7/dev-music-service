{
  "builds": [
    {
      "src": "api/index.py",
      "use": "@vercel/python"
    }
  ],
  "installCommand": "apt-get update && apt-get install -y ffmpeg && pip install --upgrade pip && pip install -r requirements.txt",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "api/index.py"
    }
  ]
}
