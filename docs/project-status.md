# Project Status

`dev-music-service` is an active FastAPI music service with browser-first
playback, optional local `ffplay` controls, Spotify playlist import, lyrics, and
an MCP server wrapper.

The current runtime contract is documented in `README.md`; operational notes are
tracked in `status.md`.
