# Project Backlog

Near-term backlog lives in `backlog.md`.

Current priorities:

- Keep verification gates green before behavior changes.
- Replace live-network test skips with fixtures.
- Harden stream target validation and process-affecting routes.
- Extract the frontend into static assets without changing behavior.
