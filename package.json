{
  "name": "dev-music-service-workspace",
  "private": true,
  "scripts": {
    "build:lyrics-shader-lab": "npm --prefix lyrics-shader-lab run build",
    "dev:lyrics-shader-lab": "npm --prefix lyrics-shader-lab run dev",
    "install:lyrics-shader-lab": "npm --prefix lyrics-shader-lab install",
    "build:canvas": "node scripts/build-canvas-surface.mjs",
    "build:semi": "node scripts/build-semi-surface.mjs",
    "sanitize:plugins": "node scripts/sanitize-plugin-surface.mjs",
    "build:vercel": "node scripts/build-vercel-frontend.mjs",
    "audit:design": "node scripts/design-audit.mjs"
  },
  "dependencies": {
    "@vercel/functions": "^3.9.3"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "@vercel/config": "^0.6.1",
    "playwright-core": "^1.62.1"
  }
}
